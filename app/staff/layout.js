export const metadata = {
  title: 'Staff Dashboard | Cafe Management',
  description: 'Staff Dashboard for Cafe Management System',
};

export default function StaffLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
