export const metadata = {
  title: 'Manager Dashboard | Cafe Management',
  description: 'Manager Dashboard for Cafe Management System',
};

export default function ManagerLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
